using System;

class CSharpDasar {
    static void Main(string[] args) {
        int angka  10;
        string.hasil = (angka % 2 == 0) ? "Genap" : "Ganjil";
        System.Console.WriteLine(hasil);
    }
}